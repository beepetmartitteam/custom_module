<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
    <record
        id="view_purchase_down_payment_apply_wizard_form"
        model="ir.ui.view"
    >

        <field name="name">
            purchase.down.payment.apply.wizard.form
        </field>

        <field name="model">
            purchase.down.payment.apply.wizard
        </field>

        <field name="arch" type="xml">

            <form string="Apply Purchase Down Payment">

                <group>

                    <group>

                        <field
                            name="vendor_bill_id"
                        />

                        <field
                            name="partner_id"
                        />

                    </group>

                    <group>

                        <field
                            name="currency_id"
                        />

                        <field
                            name="bill_amount"
                        />

                    </group>

                </group>

                <group>

                    <group>

                        <field
                            name="already_applied"
                        />

                    </group>

                    <group>

                        <field
                            name="remaining_bill_amount"
                        />

                    </group>

                </group>

                <separator string="Available Down Payments"/>

                <field name="line_ids">

                    <list editable="bottom">

                        <field
                            name="down_payment_id"
                        />

                        <field
                            name="available_amount"
                        />

                        <field
                            name="apply_amount"
                        />

                        <field
                            name="currency_id"
                            column_invisible="True"
                        />

                    </list>

                </field>

                <footer>

                    <button
                        name="action_apply"
                        type="object"
                        string="Apply Down Payment"
                        class="btn-primary"
                    />

                    <button
                        string="Cancel"
                        special="cancel"
                        class="btn-secondary"
                    />

                </footer>

            </form>

        </field>

    </record>
    -->

</odoo>